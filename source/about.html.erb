<div id="main-wrapper">
  <div id="main" class="container">
    <div class="row">
      <div class="12u">
        <div class="content">

          <!-- Content -->

            <article class="box page-content">

              <header>
                <h2>Chi Siamo</h2>
              </header>

              <section>
                <span class="image featured"><img src="<%= dato.about.cover.file.to_url(fit: 'clip', w: 1200, h: 1200) %>" alt="Chi Siamo" /></span>
                <div class="description--strong">
                  <%= dato.about.description %>
                </div>
              </section>

            </article>

        </div>
      </div>
    </div>
    <div class="row 200%">
      <div class="12u">

        <!-- Features -->
          <section class="box features">
            <h2 class="major about__company-heading"><span>La Compagnia</span></h2>
            <div>
              <% dato.about.team_members.sort_by(&:position).each_slice(4).to_a.each do |team_row| %>
                <div class="row">
                  <% team_row.each do |team_member| %>
                    <div class="3u 12u(mobile)">
                      <section class="box feature">
                        <a href="javascript:void(0)" class="image featured"><img src="<%= team_member.cover.file.to_url(fit: 'clip', w: 1200, h: 1200) %>" alt="<%= team_member.full_name %>" /></a>
                        <h3><a href="javascript:void(0)"><%= team_member.full_name %></a></h3>
                        <div class="description--condensed">
                          <%= team_member.description %>
                        </div>
                      </section>
                    </div>
                  <% end %>
                </div>
              <% end %>
            </div>
          </section>

          <section class="box features">
            <h2 class="major"><span>Collaboratori</span></h2>
            <div>
              <% dato.about.collaborators.sort_by(&:position).each_slice(4).to_a.each do |collaborators_row| %>
                <div class="row">
                  <% collaborators_row.each do |collaborator| %>
                    <div class="3u 12u(mobile)">
                      <section class="box feature">
                        <a href="javascript:void(0)" class="image featured"><img src="<%= collaborator.cover.file.to_url(fit: 'clip', w: 1200, h: 1200) %>" alt="<%= collaborator.name %>" /></a>
                        <h3><a href="javascript:void(0)"><%= collaborator.name %></a></h3>
                        <div class="description--condensed">
                          <%= collaborator.description %>
                        </div>
                      </section>
                    </div>
                  <% end %>
                </div>
              <% end %>
            </div>
          </section>

      </div>
    </div>
  </div>
</div>
